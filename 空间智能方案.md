## 核心 Motivation：VLM 的空间能力并非缺失，而是“延迟形成且难以保持”

我们的 probing 揭示了 VLM 中空间表征的一条清晰演化路径。

### 1. Vision Encoder 具有局部几何，但缺少跨帧组织

Qwen 的视觉编码器逐帧独立处理图像，因此能提取单目深度、透视和局部结构，却无法直接比较不同视角。相较于 Vision Encoder，LLM 内部表现为：

| 指标 | Vision Encoder 最优 | LLM 最优 |
|---|---:|---:|
| Point-map MSE $\downarrow$ | 0.306 | **0.186** |
| Pose AUC$_{30}$ $\uparrow$ | 0.217 | **0.417** |
| Instance mIoU $\uparrow$ | 0.260 | **0.270** |
| Depth AbsRel $\downarrow$ | 0.0895 | 0.0891 |

进入 LLM 后，提升主要集中在 point map、相机位姿和跨视图 grouping，而单目深度几乎不变。这表明：

> Vision Encoder 提供逐帧几何线索，而 LLM 中不同帧视觉 token 的因果注意力进一步将其组织为跨视图几何。

严格来说，这是 probing 支持的机制假设；可通过阻断跨帧 attention 进一步验证因果性。

### 2. 几何信息分布在不同网络深度

不同空间属性的最佳层并不相同：

- Vision Encoder 的有效几何集中在中后层；
- depth 和 grouping 在 LLM 较早层较强；
- point map 和 pose 在 LLM 中层达到峰值。

因此，空间表示并不是某一层的静态输出，而是在 VLM 深度上逐步形成。

### 3. 形成后的几何在 LLM 后层发生衰减

例如，Qwen 的 Pose AUC 从中层的 $0.417$ 降至最终层的 $0.331$；SpatialStack 也表现出明显的几何衰减。由此得到核心发现：

> **VLM 的空间表征具有“延迟形成、短暂存在”的特点：局部几何始于 Vision Encoder，跨视图几何形成于 LLM 早中层，但在后期语义化过程中逐渐变得不可读。**

---

# 方法：Progressive Spatial Latent Distillation

基于上述发现，我们不再将几何 token 作为一次性前缀，而是让同一组空间 token 贯穿 Vision Encoder 与 LLM，并随着 VLM 表征逐步演化。

核心流程可以概括为：

$$
\boxed{
\text{Extract locally}
\rightarrow
\text{Consolidate globally}
\rightarrow
\text{Refine progressively}
}
$$

---

## 1. 重建预训练的空间监督

VGGT-$\Omega$ 为每帧提供：

$$
G_i=
[g_i^{\mathrm{cam}};
g_{i,1}^{\mathrm{scene}},\ldots,g_{i,K}^{\mathrm{scene}}].
$$

其中 scene tokens 是**逐帧存在、经过跨视图上下文化的紧凑 latent**。VGGT-$\Omega$ 的下游实验表明，仅使用这些 scene tokens 就能增强语言、行动等任务，说明它们不仅是计算 register，也是具有迁移价值的空间表示。

因此，我们蒸馏的是由三维重建预训练形成的 compact spatial state，而不是 VGGT-$\Omega$ 的 dense patch features 或显式重建输出。

Camera 和 scene tokens 只是互补的监督目标：

- camera token 强调视角、相机位姿与运动；
- scene tokens 提供更广泛的场景结构和跨视图空间信息；

方法不人为规定二者之间的特殊关系。

---

## 2. Alternating Spatial Refiner

每帧维护一组学生空间 token：

$$
Z_i=
[z_i^{\mathrm{cam}};z_{i,1:K}^{\mathrm{scene}}].
$$

每次 refinement 只有两步。

### 帧内提取

空间 token 读取对应帧的特征：

$$
\widetilde Z_i^{(m)}
=
Z_i^{(m-1)}
+
\operatorname{CrossAttn}
\left(
Z_i^{(m-1)},X_i^{(m)}
\right).
$$

它从当前网络深度提取单帧空间证据。

### 跨帧整合

所有帧的 compact spatial tokens 进行双向全局注意力：

$$
Z^{(m)}
=
\widetilde Z^{(m)}
+
\operatorname{GlobalAttn}
\left(
[\widetilde Z_1^{(m)};\ldots;\widetilde Z_T^{(m)}]
\right).
$$

其核心原则是：

> **Geometry is observed locally but resolved globally.**

由于全局注意力只作用在少量空间 token 上，而非所有视觉 patches，因此计算量很小。

---

## 3. 空间 token 贯穿 Vision Encoder 与 LLM

### Vision 阶段：初始化空间状态

根据 probing 选择若干有效的 Vision Encoder 中间层，连续执行 alternating refinement：

$$
Z^{\mathrm{vis}}
=
\operatorname{ASR}
\left(
Z^{(0)},
\{X_{\mathrm{vis}}^{(\ell_1)},
X_{\mathrm{vis}}^{(\ell_2)},\ldots\}
\right).
$$

这一步将逐帧视觉线索提前组织成初始的跨视图空间状态。

随后将它们置于对应帧视觉 token 附近：

$$
[Z_1,V_1;\ldots;Z_T,V_T;\text{Text}].
$$

### LLM 阶段：渐进更新空间状态

Probing 表明 LLM 内部的 visual states 包含更强的 point-map 和 pose 信息。因此，在少数 probing-selected LLM 层，再使用同一个 alternating operation：

1. 空间 token 读取当前层对应帧的 visual states；
2. 所有空间 token 进行双向跨帧整合；
3. 更新结果以 gated residual 加回原来的特殊 token 位置：

$$
H_Z^{(j)}
\leftarrow
H_Z^{(j)}
+
\gamma_j P_j
\operatorname{ASR}
\left(
H_Z^{(j)},H_V^{(j)}
\right).
$$

其中 $P_j$ 或 $\gamma_j$ 采用零初始化，尽量保持预训练 VLM 的初始行为。

因此，它不是在不同层反复生成新 token，而是让同一空间状态持续演化：

$$
Z^{\mathrm{vis}}
\rightarrow
Z^{(j_1)}
\rightarrow
Z^{(j_2)}
\rightarrow
Z^{\mathrm{out}}.
$$

---

## 4. 双端空间蒸馏

只在两个关键位置对齐 VGGT-$\Omega$：

### Vision–LLM 边界

$$
\mathcal L_{\mathrm{in}}
=
d\left(D_{\mathrm{in}}(Z^{\mathrm{vis}}),G\right).
$$

保证模型在进入 LLM 前已经形成明确的空间状态。

### LLM 输出端

$$
\mathcal L_{\mathrm{out}}
=
d\left(D_{\mathrm{out}}(Z^{\mathrm{out}}),G\right).
$$

保证经过完整语言计算后，空间知识仍然可以被读取。

总损失为：

$$
\mathcal L
=
\mathcal L_{\mathrm{SFT}}
+
\lambda_{\mathrm{in}}\mathcal L_{\mathrm{in}}
+
\lambda_{\mathrm{out}}\mathcal L_{\mathrm{out}}.
$$

这不再需要额外的逐层 Persistent Spatial Distillation：空间 token 已经通过结构化 refinement 被持续刷新，而输出端蒸馏负责约束最终保留。

推理时删除 VGGT-$\Omega$ 和所有蒸馏头，只保留轻量空间 refiner。

---

# Probing 发现与设计的直接对应

| Probing 发现 | 方法设计 |
|---|---|
| Vision Encoder 逐帧处理，局部深度强但 pose、point map 弱 | 帧内 cross-attention 提取单帧空间证据 |
| LLM 对跨视图指标提升明显 | 对 compact spatial tokens 做跨帧 global attention |
| 几何信息分布在 Vision Encoder 和 LLM 的多个层级 | Spatial tokens 贯穿两部分并渐进更新 |
| LLM 中层几何强于 Vision Encoder | Refiner 不止作用于视觉编码器，也读取 LLM visual states |
| 后期 LLM 出现 geometry washout | 空间 token 被周期性刷新，并在输出端再次蒸馏 |
| VGGT-$\Omega$ scene tokens 紧凑且可迁移 | 以 camera/scene latents 为教师，而非 dense geometry |
| VLM 本身语义能力已经很强 | 保留原始 visual stream，只增加少量空间状态 |

---

## 最终定位

> **现有 VLM 已经隐式形成空间信息，但这一过程发生得较晚、依赖非对称的因果跨帧交互，并且形成的几何在后层不稳定。我们让一组紧凑空间 token 贯穿整个 VLM，通过交替的帧内提取和跨帧整合持续演化，并利用 VGGT-$\Omega$ 的重建空间 latent 在视觉边界与语言输出两端对其进行监督。**

最凝练的核心 insight 是：

$$
\boxed{
\text{Spatial representations should evolve with the VLM, rather than be injected only once.}
}
$$

这套方法的重点不再是压缩 VGGT-$\Omega$，也不是建模 camera–scene token 之间的人为关系，而是：

> **将 VLM 中分散、延迟且短暂的几何线索，转化为贯穿视觉与语言计算过程的、持续演化的显式空间状态。** 
> 